fn fact(n) {
    if (n < 2) {
        return 1;
    }
    return n * fact(n - 1);
}

fn fibo(n) {
    if(n == 1) {
        return n;
    }
    if(n < 1) {
        return n;
    }
    return fibo(n-1) + fibo(n-2);
}

fn main() {
    print(fact(5));
    print(fibo(8));
}
